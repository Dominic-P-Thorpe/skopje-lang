fn collatz(n: u32) -> u32 {
    let i: u32 = 0;
    while (n != 1) {
        n = n % 2 == 0 ? n / 2 : n * 3 + 1;
        i = i + 1;
    }

    return i;
}


fn main() -> i32 {
    print(int2str(collatz(40767)));
    return 1;
}
