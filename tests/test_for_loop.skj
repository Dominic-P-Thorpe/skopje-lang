fn main() -> i32 {
    for (i: u32 in 0..100) {
        print("Hello world!");
    }

    let my_array: str[6] = ["Tom", "Jerry", "Bugs", "Cat in the Hat", "Bluey", "Moomintroll"];
    for (iterator: str in my_array) {
        print(iterator);
    }

    let matrix: u32[3][3] = [
        [1, 2, 3],
        [4, 5, 6],
        [7, 8, 9]
    ];

    let mut total: u64 = 0;
    for (row: u32[3] in matrix) {
        for (col: u32 in row) {
            total = total + col;
        }
    }

    return 1;
}
